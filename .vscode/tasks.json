{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd ddb2; /usr/lib64/ccache/gcc -Wall -g ddb.c bytecode.c tokens.c -o ../builds/ddb2",
            
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}